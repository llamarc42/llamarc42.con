# Preserved upstream workflows

These workflows were inherited from Continue. They include marketplace publishing,
upstream repository destinations, scheduled mutations, and service credentials.
They are preserved here for reference and do not run from this directory.

The fork's active workflows live in `.github/workflows/`. Restore or port an
inherited workflow only after reviewing its destinations, permissions, credentials,
and execution scope. Do not enable all inherited workflows as a way to enable CI.
